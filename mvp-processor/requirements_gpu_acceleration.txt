# GPU Hardware Acceleration Requirements
# Install these packages to enable 5-10x performance improvements

# Core GPU acceleration libraries
torch>=2.0.0                    # PyTorch for GPU operations
torchvision>=0.15.0            # Computer vision utilities

# Apple Silicon Metal Performance Shaders (macOS only)
# torch will automatically include MPS support on compatible systems

# NVIDIA CUDA support (if using NVIDIA GPU)
# Install CUDA-compatible PyTorch version:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# FAISS for fast similarity search
faiss-cpu>=1.7.4               # CPU version (always install first)
# faiss-gpu>=1.7.4             # GPU version (install if CUDA available)
# Install command for FAISS GPU: pip install faiss-gpu

# Numba for JIT compilation and vectorized operations
numba>=0.58.0                  # Just-in-time compilation for 2-3x speedup
llvmlite>=0.41.0              # LLVM dependency for Numba

# Performance monitoring and optimization
psutil>=5.9.0                  # System resource monitoring
threadpoolctl>=3.1.0          # Thread pool control for optimization

# ONNX Runtime for additional acceleration options
onnxruntime>=1.15.0           # CPU version
# onnxruntime-gpu>=1.15.0     # GPU version (install if CUDA available)

# Optional: Intel MKL for CPU optimization
# mkl>=2023.0                  # Intel Math Kernel Library (CPU acceleration)

# Optional: TensorRT for NVIDIA GPU optimization
# tensorrt>=8.6.0             # NVIDIA TensorRT (requires CUDA)

# CoreML Tools for Apple Silicon optimization (macOS only)
coremltools>=7.0; sys_platform == "darwin"  # Apple CoreML integration

# Memory optimization utilities
memory-profiler>=0.61.0       # Memory usage profiling and optimization

# Additional performance utilities
scipy>=1.10.0                 # Scientific computing optimizations
scikit-learn>=1.3.0          # Machine learning utilities with optimized operations

# Hardware detection and system info
py-cpuinfo>=9.0.0            # Detailed CPU information
GPUtil>=1.4.0                # GPU utilization monitoring (NVIDIA)

# Optional: Additional acceleration libraries
# cupy-cuda11x>=12.0.0        # CuPy for GPU-accelerated NumPy operations
# rapids-cudf>=23.0.0         # RAPIDS for GPU-accelerated data processing