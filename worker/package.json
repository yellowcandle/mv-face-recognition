{
  "name": "mv-face-recognition-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker for MV Face Recognition API",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230922.0",
    "@types/node": "^20.5.9",
    "eslint": "^8.48.0",
    "miniflare": "^3.20230918.0",
    "prettier": "^3.0.3",
    "vitest": "^0.34.6",
    "vitest-environment-miniflare": "^2.14.4",
    "wrangler": "^3.10.1"
  }
}
