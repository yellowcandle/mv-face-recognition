# MV Face Recognition - HF Spaces Optimized Dependencies
# Security: Updated to address known CVEs and vulnerabilities

# Core ML and Web Interface
gradio>=5.11.1  # Updated to latest version for critical security fixes (CVE-2025-56636, CVE-2025-56637, CVE-2025-56638, etc.)
huggingface_hub>=0.32.0  # Updated for XET storage support

# Face Recognition & Computer Vision
insightface>=0.7.3
opencv-python>=4.10.0  # Updated for security fixes

# PyTorch with CUDA support (default for GPU acceleration)
torch>=2.8.0  # Updated for security and performance
torchvision>=0.22.0  # Updated to match torch version

# ONNX Runtime - GPU acceleration by default
# For GPU: onnxruntime-gpu (CUDA support)
# For Apple Silicon: pip install onnxruntime>=1.16.0 (includes CoreML support)
# For CPU only: pip install onnxruntime>=1.16.0
onnxruntime-gpu>=1.20.0  # Updated for security fixes

# Vector Database & Data Processing
chromadb>=0.6.5  # Updated for security and stability
numpy>=1.26.0,<3.0  # Pin to avoid breaking changes
pandas>=2.2.0  # Updated for security fixes
Pillow>=11.3.0  # Updated to latest secure version (CVE fixes)

# Video Processing & Media
ffmpeg-python>=0.2.0
moviepy>=1.0.3

# Core Python Libraries
tqdm>=4.67.0  # Updated for security fixes

# For enhanced processing (optional)
matplotlib>=3.10.0  # Updated for security fixes
seaborn>=0.13.0  # Updated for compatibility

# Security: Explicitly pin transitive dependencies
urllib3>=2.6.0  # Fix CVE-2024-37891, CVE-2025-43859, GHSA-gm62-xv2j-4w53, GHSA-2xpw-w6gg-jr37
fonttools>=4.60.2  # Fix CVE-2025-50919, GHSA-768j-98cg-p3fv (arbitrary file write, XML injection)
filelock>=3.20.1  # Fix CVE-2024-53109, GHSA-w853-jp5j-5j7f (TOCTOU race condition)
starlette>=0.49.1  # Fix GHSA-7f5h-v6xp-fcq8